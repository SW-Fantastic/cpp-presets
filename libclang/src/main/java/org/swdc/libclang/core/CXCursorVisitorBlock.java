// Targeted by JavaCPP version 1.5.10: DO NOT EDIT THIS FILE

package org.swdc.libclang.core;

import java.nio.*;
import org.bytedeco.javacpp.*;
import org.bytedeco.javacpp.annotation.*;

import org.swdc.libclang.core.io.*;
import static org.swdc.libclang.core.io.ClangIO.*;

import static org.swdc.libclang.core.LibClang.*;

/**
 * Visitor invoked for each cursor found by a traversal.
 *
 * This visitor block will be invoked for each cursor found by
 * clang_visitChildrenWithBlock(). Its first argument is the cursor being
 * visited, its second argument is the parent visitor for that cursor.
 *
 * The visitor should return one of the \c CXChildVisitResult values
 * to direct clang_visitChildrenWithBlock().
 */
// #if __has_feature(blocks)
@Properties(inherit = org.swdc.libclang.conf.LibClangConfigure.class)
public class CXCursorVisitorBlock extends FunctionPointer {
    static { Loader.load(); }
    /** Pointer cast constructor. Invokes {@link Pointer#Pointer(Pointer)}. */
    public    CXCursorVisitorBlock(Pointer p) { super(p); }
    protected CXCursorVisitorBlock() { allocate(); }
    private native void allocate();
    public native @Cast("CXChildVisitResult") int call(@ByVal CXCursor cursor,
                                                        @ByVal CXCursor parent);
}
